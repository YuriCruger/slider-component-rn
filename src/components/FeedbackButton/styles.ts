import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    position: "absolute",
    top: "50%",
    left: 10,
    right: 10,
    bottom: 0,
    maxHeight: 50,
    minHeight: 50,
    width: "100%",
    maxWidth: 140,
    borderRadius: 100,
    alignItems: "center",
    justifyContent: "center",
  },
  feedbackButtonText: {
    color: "#ffffff",
  },
});
