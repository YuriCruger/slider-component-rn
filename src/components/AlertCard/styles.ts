import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#FEF6CC",
    borderRadius: 8,
    padding: 12,
    borderWidth: 1,
    borderColor: "#F1B304",
    marginBottom: 16,
  },
  textBold: {
    fontWeight: "700",
  },
  rowGroup: {
    flexDirection: "row",
    gap: 6,
  },
  textContainer: {
    flexShrink: 1,
  },
});
