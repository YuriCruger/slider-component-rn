import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mapView: {
    flex: 1,
    minHeight: "100%",
    justifyContent: "flex-end",
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
});
